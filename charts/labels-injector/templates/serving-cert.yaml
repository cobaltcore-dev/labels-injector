apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "labels-injector.fullname" . }}-serving-cert
  labels:
  {{- include "labels-injector.labels" . | nindent 4 }}
spec:
  dnsNames:
  - '{{ include "labels-injector.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc'
  - '{{ include "labels-injector.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc.{{ .Values.kubernetesClusterDomain }}'
  issuerRef:
    kind: Issuer
    name: '{{ include "labels-injector.fullname" . }}-selfsigned-issuer'
  secretName: webhook-server-cert